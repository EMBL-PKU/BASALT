# Read the Docs build configuration.
# Paths are relative to the repository root.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: BASALT_Guide/docs/conf.py
  fail_on_warning: true

python:
  install:
    - requirements: BASALT_Guide/requirements.txt
